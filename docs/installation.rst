============
Installation
============

At the command line::

    $ easy_install rinno_material

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rinno_material
    $ pip install rinno_material
